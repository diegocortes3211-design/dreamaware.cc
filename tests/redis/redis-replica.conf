# redis-replica.conf
# The `replicaof` directive points to the primary node.
# 'redis-primary' is the service name we will use in docker-compose.
replicaof redis-primary 6379
appendonly yes
maxmemory-policy noeviction