# Fallout Fuzz workflow
on:
  schedule:
    - cron: "0 4 * * *"  # daily 04:00 UTC
  workflow_dispatch:

jobs:
  fuzz:
    runs-on: ubuntu-latest
    steps:
      - name: Run Fuzzing
        run: echo "Fuzzing..."